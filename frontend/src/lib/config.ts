export const SIMILARITY_THRESHOLD = 0.3;
export const EMBEDDING_MODEL = "text-embedding-3-small";

// Prototype affordance — one line to kill before backend wiring.
export const SHOW_STATE_SWITCHER = true;
